{
  "name": "ics-to-gcal",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Firefox extension that converts local or opened ICS events into Google Calendar edit links.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "package": "npm run build && python3 scripts/package.py",
    "test": "node --test",
    "check": "npm test && npm run package"
  },
  "engines": {
    "node": ">=18"
  }
}
